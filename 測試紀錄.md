# 測試紀錄

記錄每次實際遊玩原型後發現的問題與觀察，依處理狀態分類：

- **已確認問題，已修改**：核對過原始碼確實有問題，且已經修正。
- **已確認問題，待修改**：核對過原始碼確實有問題，但尚未修改。
- **待觀察**：目前數值符合規格文件或原型階段的合理預設，但遊玩體感有疑慮，需要更多樣本才能判斷是否要調整。
- **非問題、純紀錄**：純粹紀錄現象／想法，不代表原型有缺陷。

---

## v0.0.2

回報日期：2026-09-14

### 已確認問題，已修改

1. **新馬賽（第3回合）目前不是強制參加**（承 v0.0.1 問題2）
   修改內容：`js/ui.js` 的 `renderCareerMain()` 新增 `isMandatoryRace` 判斷（`scheduledRace.grade === "maiden"` 時成立）。成立時不再渲染訓練按鈕與休息按鈕，只留「出賽：新馬戰」一個可點擊行動，並顯示提示文字「新馬戰是生涯必經的第一戰，本回合必須出賽，無法訓練或休息。」由於所有行動都是透過畫面上的按鈕觸發（`main.js` 的點擊委派處理），拿掉按鈕即等同拿掉該回合能繞過出賽的路徑；出賽預覽畫面的「取消，返回」按鈕也只會繞回同樣只剩出賽選項的主畫面，不構成漏洞。
   驗證：`node test/smoke.js`（300輪無UI模擬）與 `node test/dom-smoke.js`（jsdom全流程點擊模擬）皆正常跑完；另外用獨立腳本直接呼叫 `getScheduledRace()` 確認第3回合固定回傳 `grade: "maiden"` 的新馬戰，行為符合預期。
   同時新增 `GAME_VERSION`（`js/constants.js`）並顯示於標題畫面，方便之後對照版本回報問題。

### 已確認問題，待修改

1. **疲勞累積過快、休息回復不足**（沿用 v0.0.1 問題1，見下方，尚未修改）

2. **新馬賽（第3回合）對不良／普通馬過難拿第一 —— 已定案修改方案，待實作**
   回報：新馬戰安排在第3回合，對於不良馬與普通馬來說，由於數值有限很難拿到第一。
   核對結果：追出兩個共同作用、但都各自符合規格文件的機制，疊加後讓新馬戰難度偏高：
   （1）`ai-opponents.md` 規定新馬賽的AI對手品質分布與玩家馬相同（不良50%／普通30%／優良15%／稀有5%），玩家馬並沒有先天優勢；
   （2）`ai-opponents.md` 另外規定AI的「主戰位置」常態分布會以該場賽事距離為平均值生成（新馬戰=短距離，平均值1.0），等於AI天生比較貼合這場比賽的距離適性；但玩家馬的主戰位置一律用通用平均值2.5生成（不會因為即將面對哪場比賽而調整），沒有對應的適性優勢。
   用專案現有邏輯跑了3000輪模擬（玩家馬在第1、2回合都對最高項目做標準訓練後直接進新馬戰，貼近常見打法）：整體拿第一名機率僅7.0%（前三名21.6%），且連「優良」「稀有」等級馬第一名機率也只有10.5%／13.2%，並沒有大幅高過不良馬的5.6%／普通馬的6.6%——遠低於「6匹馬中憑實力應有機會拿到約1/6～更高」的直覺預期。數字確實偏低，但這是規格文件各自獨立成立、疊加後才顯現的難度，不是撰寫錯誤。

   **正式定案方案（2026-09-14）：方向C + 方案1**
   曾比較過三個調整方向——方向A調低AI難度、方向B延長總回合數並延後新馬戰時機、方向C新馬戰改四種距離自選——最終選定方向C，理由是它直接解決「AI適性針對賽事距離特化、玩家馬沒有對應優勢」這個根本落差，而不是把關卡難度整體調低或繞開問題。方向C確定後，接著比較未勝利賽（第6、8回合）要「沿用新馬賽選擇的距離」（方案1）還是「跟新馬賽一樣每次都重新四選一」（方案2），最終選定方案1，理由是它呼應 `horse-generation.md` 距離傾向軸「一次生成、影響一輩子」的設計精神，也避免方案2容易退化成「每次都挑最好打的距離重選」、實質效果跟方案1差異不大但要多付出UI與測試成本。

   **定案後的規格內容**：
   - 第3回合新馬戰改為同時列出短距離(1200m)／一哩(1600m)／中距離(2000m)／長距離(3000m)四個選項，玩家自行選擇要挑戰哪一種距離出賽；選定後即觸發對應距離的AI對手生成（AI主戰位置常態分布均值對應到玩家選擇的距離）。新馬戰仍是「生涯唯一一場、第3回合固定觸發、當回合強制出賽」，只有距離變成玩家自選。
   - 若新馬戰未獲勝，第6、8回合開放的未勝利賽，距離自動沿用玩家在新馬戰選擇的那個距離類別，不再另外詢問，直到獲勝解鎖公開賽資格或第10回合觸發安全機制為止。
   - 涉及改動：`race-calendar.md`「新馬賽固定短距離1200m」「未勝利賽固定一哩1600m」的文字敘述需更新；`js/state.js` 的 `getScheduledRace()` 與 `career` 狀態需新增欄位記錄玩家選擇的距離類別（例如 `chosenDistanceCat`），未勝利賽分支要改成讀這個值而不是寫死 `mile`；`js/ui.js` 需新增新馬戰的距離選擇畫面/按鈕組，取代目前單一顆「出賽：新馬戰」按鈕；`test/dom-smoke.js` 需涵蓋「新馬戰選任一距離 → 未勝利賽（若觸發）自動套用同距離」的路徑。
   - 尚未動手：這裡只記錄定案的設計方向，實際程式修改與版本號更新留到下一個開發回合再進行。

### 待觀察

3. **正常跑完36回合的生涯比例仍偏低**（延續 v0.0.1 問題3的觀察）
   回報：目前已經有出現完整跑完正常流程（未提前結束）的生涯紀錄，但相較於提前結束的生涯，比例仍然偏低，需要繼續觀察。
   核對結果：與 v0.0.1 問題3（稀有事件觸發率）是同一組現象的延伸樣本，暫不重複驗證機率算式；目前樣本數仍不足以判斷是稀有事件機率、疲勞回復機制（問題1），或兩者疊加造成的效果，先持續累積遊玩紀錄再一併判斷。

### 非問題、純紀錄

（無）

---

## v0.0.1

回報日期：2026-09-14

### 已確認問題，已修改

（無）

### 已確認問題，待修改

1. **疲勞累積過快、休息回復不足**
   回報：目前疲勞值累積後，靠休息只能減少一些疲勞值，且很容易在訓練後再度累積到較高值，希望調整休息時疲勞降低的幅度。
   核對結果：`js/constants.js` 中休息一次疲勞 −25，標準訓練 +18、強化訓練 +28，且疲勞沒有隨回合自然下降的機制，只能靠休息或少數正面事件降低。休息一次的效果幾乎會被緊接著一次訓練打平甚至倒賺，問題確實存在。

2. **新馬賽（第3回合）目前不是強制參加** → 已於 v0.0.2 修改，見上方。
   回報：希望修改成第3回合必須參加新馬賽。
   核對結果：`js/state.js` 的 `getScheduledRace()` 只回傳「這回合有沒有賽事可選」，`ui.js` 生涯主畫面把它跟訓練／休息並列成可選按鈕，玩家可以在第3回合選擇跳過新馬賽，目前沒有任何強制邏輯，問題確實存在。

### 待觀察

3. **提前結束生涯的稀有事件觸發率是否偏高**
   回報：目前遊玩的兩個紀錄都因事件（重大傷病／天災意外／醜聞風波／伯樂相中其中之一）提前終止生涯，需要持續觀察觸發率是否過高。
   核對結果：`js/events.js` 中這組事件合計機率為 0.8%／回合，與 `events.md` 規格文件數字一致，並非計算或撰寫錯誤。已確認疲勞≥95 的強制引退判定是獨立於此的另一條路徑（且不會跳事件彈窗），所以兩次記錄應該真的是觸發到這組 0.8% 的稀有事件，不是誤判。以平均遊玩 15~20 回合估算，累積觸發機率約一到兩成，兩輪都中不算離譜但樣本數（n=2）太小，無法判斷是數值偏高還是純粹運氣不好。另外問題1（疲勞回復不足）如果讓玩家被迫更常做高風險行動，可能間接墊高踩到稀有事件的機會，之後可以跟問題1一併觀察。

### 非問題、純紀錄

（無）
