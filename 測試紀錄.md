# 測試紀錄

記錄每次實際遊玩原型後發現的問題與觀察，依處理狀態分類（不分日期／版本集中呈現，每筆紀錄底下標註「紀錄版本」註明是哪個版本回報、哪個版本修改）：

- **已確認問題，已修改**：核對過原始碼確實有問題，且已經修正。
- **已確認問題，待修改**：核對過原始碼確實有問題，但尚未修改。
- **待觀察**：目前數值符合規格文件或原型階段的合理預設，但遊玩體感有疑慮，需要更多樣本才能判斷是否要調整。
- **非問題、純紀錄**：純粹紀錄現象／想法，不代表原型有缺陷。

---

## 已確認問題，已修改

1. **新馬賽（第3回合）目前不是強制參加**
   紀錄版本：v0.0.1 回報 → v0.0.2 修改完成

   回報（v0.0.1）：希望修改成第3回合必須參加新馬賽。
   核對結果（v0.0.1）：`js/state.js` 的 `getScheduledRace()` 只回傳「這回合有沒有賽事可選」，`ui.js` 生涯主畫面把它跟訓練／休息並列成可選按鈕，玩家可以在第3回合選擇跳過新馬賽，目前沒有任何強制邏輯，問題確實存在。
   修改內容（v0.0.2）：`js/ui.js` 的 `renderCareerMain()` 新增 `isMandatoryRace` 判斷（`scheduledRace.grade === "maiden"` 時成立）。成立時不再渲染訓練按鈕與休息按鈕，只留「出賽：新馬戰」一個可點擊行動，並顯示提示文字「新馬戰是生涯必經的第一戰，本回合必須出賽，無法訓練或休息。」由於所有行動都是透過畫面上的按鈕觸發（`main.js` 的點擊委派處理），拿掉按鈕即等同拿掉該回合能繞過出賽的路徑；出賽預覽畫面的「取消，返回」按鈕也只會繞回同樣只剩出賽選項的主畫面，不構成漏洞。同時新增 `GAME_VERSION`（`js/constants.js`）並顯示於標題畫面，方便之後對照版本回報問題。
   驗證（v0.0.2）：`node test/smoke.js`（300輪無UI模擬）與 `node test/dom-smoke.js`（jsdom全流程點擊模擬）皆正常跑完；另外用獨立腳本直接呼叫 `getScheduledRace()` 確認第3回合固定回傳 `grade: "maiden"` 的新馬戰，行為符合預期。

2. **新馬賽（第3回合）對不良／普通馬過難拿第一**
   紀錄版本：v0.0.2 回報並定案 → v0.0.3 修改完成

   回報（v0.0.2）：新馬戰安排在第3回合，對於不良馬與普通馬來說，由於數值有限很難拿到第一。
   核對結果（v0.0.2）：追出兩個共同作用、但都各自符合規格文件的機制，疊加後讓新馬戰難度偏高：
   （1）`ai-opponents.md` 規定新馬賽的AI對手品質分布與玩家馬相同（不良50%／普通30%／優良15%／稀有5%），玩家馬並沒有先天優勢；
   （2）`ai-opponents.md` 另外規定AI的「主戰位置」常態分布會以該場賽事距離為平均值生成（新馬戰=短距離，平均值1.0），等於AI天生比較貼合這場比賽的距離適性；但玩家馬的主戰位置一律用通用平均值2.5生成（不會因為即將面對哪場比賽而調整），沒有對應的適性優勢。
   用專案現有邏輯跑了3000輪模擬（玩家馬在第1、2回合都對最高項目做標準訓練後直接進新馬戰，貼近常見打法）：整體拿第一名機率僅7.0%（前三名21.6%），且連「優良」「稀有」等級馬第一名機率也只有10.5%／13.2%，並沒有大幅高過不良馬的5.6%／普通馬的6.6%——遠低於「6匹馬中憑實力應有機會拿到約1/6～更高」的直覺預期。數字確實偏低，但這是規格文件各自獨立成立、疊加後才顯現的難度，不是撰寫錯誤。

   **正式定案方案（v0.0.2，2026-09-14）：方向C + 方案1**
   曾比較過三個調整方向——方向A調低AI難度、方向B延長總回合數並延後新馬戰時機、方向C新馬戰改四種距離自選——最終選定方向C，理由是它直接解決「AI適性針對賽事距離特化、玩家馬沒有對應優勢」這個根本落差，而不是把關卡難度整體調低或繞開問題。方向C確定後，接著比較未勝利賽（第6、8回合）要「沿用新馬賽選擇的距離」（方案1）還是「跟新馬賽一樣每次都重新四選一」（方案2），最終選定方案1，理由是它呼應 `horse-generation.md` 距離傾向軸「一次生成、影響一輩子」的設計精神，也避免方案2容易退化成「每次都挑最好打的距離重選」、實質效果跟方案1差異不大但要多付出UI與測試成本。

   **定案後的規格內容（v0.0.2）**：
   - 第3回合新馬戰改為同時列出短距離(1200m)／一哩(1600m)／中距離(2000m)／長距離(3000m)四個選項，玩家自行選擇要挑戰哪一種距離出賽；選定後即觸發對應距離的AI對手生成（AI主戰位置常態分布均值對應到玩家選擇的距離）。新馬戰仍是「生涯唯一一場、第3回合固定觸發、當回合強制出賽」，只有距離變成玩家自選。
   - 若新馬戰未獲勝，第6、8回合開放的未勝利賽，距離自動沿用玩家在新馬戰選擇的那個距離類別，不再另外詢問，直到獲勝解鎖公開賽資格或第10回合觸發安全機制為止。

   修改內容（v0.0.3）：
   - `js/constants.js`：`GAME_VERSION` 更新為 `v0.0.3`；`RACE_CALENDAR_FIXED` 移除第3回合的固定距離項目（改由下方邏輯動態決定，此表原本的第3回合項目其實從未被讀到，因為 `getScheduledRace()` 一律優先處理 turn===3 分支，一併順手清掉避免誤導）。
   - `js/state.js`：新增共用常數 `DISTANCE_CAT_METERS`（原 `RETIREMENT_DISTANCE_MAP` 改為它的別名，避免動到 `ui.js` 既有引用）；`newCareer()` 新增 `chosenDistanceCat` 欄位（初始 `null`）；`getScheduledRace()` 第3回合改回傳 `distanceCat: null, distance: null`（留給玩家在預覽畫面選擇），第6/8回合未勝利賽分支改為讀取 `career.chosenDistanceCat`（無值時退回 `mile`，理論上不會發生，因為新馬戰必定先觸發並寫入這個欄位）；`doRaceAction()` 新增第4個參數 `maidenDistanceCat`，`grade === "maiden"` 時依此參數（或已記錄的 `chosenDistanceCat`、皆無則退回 `short`）決定距離並寫回 `career.chosenDistanceCat`。
   - `js/ui.js`：`renderRacePreview()` 新增 `def.grade === "maiden"` 分支，比照原本引退紀念賽的距離選擇UI，渲染短/一哩/中/長四個按鈕（`data-action="select-maiden-distance"`）；未選擇距離前「確認出賽」按鈕加上 `disabled`，畫面提示「尚未選擇距離」；`UI.state` 新增 `maidenDistanceCat`（初始 `null`）。
   - `js/main.js`：新增 `select-maiden-distance` 的 `handleAction` 分支（寫入 `UI.state.maidenDistanceCat`）；`go-race-preview` 進畫面時重置 `UI.state.maidenDistanceCat = null`；`handleConfirmRace()` 呼叫 `doRaceAction` 時多傳入 `UI.state.maidenDistanceCat`。
   - `race-calendar.md`：更新新馬賽／未勝利賽的文字敘述與日曆表格，反映距離改玩家自選、未勝利賽沿用新馬戰距離。
   - `test/dom-smoke.js`：新增邏輯，`racePreview` 畫面若偵測到是新馬戰且尚未選距離，先模擬點擊 `select-maiden-distance` 再確認出賽。`test/smoke.js`：`doRaceAction` 呼叫多傳入隨機距離作為 `maidenDistanceCat`。
   驗證（v0.0.3）：`node test/smoke.js`（300輪無UI模擬，0 errors）與 `node test/dom-smoke.js`（jsdom全流程點擊模擬，含新馬戰選距離→比賽→事件→引退結算的完整路徑）皆正常跑完；另外用獨立腳本直接呼叫 `getScheduledRace()`／`doRaceAction()` 確認：第3回合 `distanceCat` 確實為 `null`（不再預設短距離）；玩家選擇長距離後 `career.chosenDistanceCat` 正確記錄為 `"long"`；未獲勝時第6回合的未勝利賽確實沿用同一距離類別（`distanceCat: "long", distance: 3000`），行為符合定案規格。
   尚未做（v0.0.3）：本次只處理了「距離改自選」的規則實作，改動後新的難度曲線（例如玩家改選自己適性較好的距離後，各品質等級馬匹的拿名機率是否回到合理範圍）尚未重新跑機率模擬驗證，留待下一輪測試紀錄再觀察補上。

   補做驗證（2026-09-16，純數據驗證，未改動程式碼，無版本更迭）：用獨立腳本跑了兩組各3000輪×4個品質等級（共24000輪）的模擬，比較「自選最擅長距離」（v0.0.3後玩家實際可用的策略：先看馬匹距離適性格線，出賽時選格線等級最高的那個距離）跟「固定短距離」（v0.0.2的舊模擬條件，作對照組）兩種打法下的新馬戰第一名／前三名機率，其餘模擬條件不變（第1、2回合皆對最高項目做標準訓練後直接進新馬戰）：

   | 品質等級 | 固定短距離 第一名% | 固定短距離 前三名% | 自選最佳距離 第一名% | 自選最佳距離 前三名% |
   |---|---|---|---|---|
   | 不良 | 5.5% | 19.8% | 32.6% | 71.7% |
   | 普通 | 8.4% | 26.0% | 39.7% | 77.7% |
   | 優良 | 10.9% | 30.6% | 46.5% | 82.2% |
   | 稀有 | 15.1% | 36.3% | 52.6% | 86.5% |
   | 整體加權平均 | 7.7% | 24.1% | 37.8% | 75.8% |

   （整體加權平均依 `horse-generation.md` 的品質機率權重：不良50%／普通30%／優良15%／稀有5%）

   「固定短距離」欄位跟 v0.0.2 原始模擬結果（整體7.0%／21.6%）數字相近，可視為對照基準沒有偏移，模擬方法一致。「自選最佳距離」欄位顯示 v0.0.3 的距離自選機制確實大幅解決了原本回報的問題——即使是不良馬，第一名機率也從5.5%大幅提升到32.6%。

   但同時發現一個新的現象值得記錄：自選最佳距離後，不良馬的第一名機率（32.6%）已經明顯超過6匹馬公平隨機下的直覺基準（1/6≈16.7%），甚至跟原本被視為「應該較強」的稀有馬固定短距離時的機率（15.1%）相比還要高出一倍以上，且四個品質等級的差距被拉近（32.6%～52.6%，範圍19個百分點，而固定短距離時是5.5%～15.1%、範圍9.6個百分點——原本的差距其實還變大了，只是整體都上移一大截）。

   原因核對：`horse-generation.md` 的距離傾向軸模型裡，四個距離分類彼此間距為1.0（短1.0／一哩2.0／中距離3.0／長距離4.0），玩家「選最擅長距離」等同於選跟自己主戰位置最近的分類，兩者差距必定 ≤0.5，對照 `APTITUDE_GRADE_TABLE`（diff≤0.3才是S、≤0.8是A），代表玩家在自選距離上幾乎穩定拿到A級以上（+5%起跳）的加成；但AI對手的主戰位置是以該距離分類的座標為平均值（標準差0.7）重新隨機生成，AI跟該距離的差距是一個隨機變數，不像玩家那樣「保證貼近」。這造成玩家在「自己選的主場」上天生比AI更容易拿到高階適性加成，是v0.0.3方向C機制本身內建的結構性優勢，不是新的程式錯誤——跟v0.0.2問題2最初核對出的落差（AI天生比玩家貼合賽事距離）方向剛好相反，變成現在換成玩家天生比AI貼合自己選的距離。

   本次不修改：使用者要求本次只進行機率驗證，不確定是否要調整、以及調整方向留待之後版本評估，故本次只記錄驗證數據與成因，不更動任何數值或程式碼，也因此不觸發版本更迭（沿用版本號規則：純驗證/純文字紀錄不算「z」）。是否要調整（例如拉近自選距離時的AI主戰位置生成、或接受這是玩家「策略性選距離」應得的合理優勢）留待下一輪觀察評估，詳見「待觀察」新增項目2。

3. **取名後選擇名聲點數兌換，原本輸入的馬名會被重置成初始名「新星」**
   紀錄版本：v0.0.3 回報 → v0.0.4 修改完成

   回報（v0.0.3）：新馬生成畫面輸入馬名後，若再勾選名聲點數兌換項目，輸入框裡的名字會被重置回初始名「新星」。
   核對結果（v0.0.3）：`js/ui.js` 的 `renderNewHorseScreen()` 裡，馬名輸入框（`#horseNameInput`）沒有繫結任何 `oninput`／`onchange` 監聽器，玩家打字的內容只存在於當下的DOM節點，沒有同步寫回 `UI.state.horseNameInput`。`js/main.js` 目前只有兩處會讀取輸入框當下的值：`regen-horse`（重新生成時讀一次）與 `confirm-new-horse`（確認開始生涯時讀一次）。而 `toggle-fame`（勾選名聲點數兌換）等其他互動都只是呼叫 `renderApp()` 整個重新渲染 `#app` 的 innerHTML，此時 `renderNewHorseScreen()` 重新產生輸入框的 `value` 屬性，用的是 `UI.state.horseNameInput || h.name`——前者從未被更新過，後者（`previewHorse.name`）預設固定是「新星」，於是玩家打的名字就被蓋掉，問題確實存在。並非兌換動作本身刻意重置，而是任何會觸發整頁重新渲染的互動都有同樣效果（例如點選跑法 `select-style` 理論上也會重現同樣問題，只是玩家比較少在打完名字後才去點跑法，所以沒被特別提及）。
   修改內容（v0.0.4）：`js/ui.js` 的 `renderNewHorseScreen()`，馬名輸入框新增 `oninput="UI.state.horseNameInput=this.value"`，讓玩家每次打字都即時同步寫回 `UI.state.horseNameInput`。這樣不管後續是勾選名聲點數兌換、點選跑法，或任何其他會呼叫 `renderApp()` 整頁重繪的互動，重繪時 `value="${escapeHtml(UI.state.horseNameInput || h.name)}"` 讀到的都會是玩家目前為止實際打的內容，而不是永遠讀不到、退回預設名「新星」。`regen-horse`／`confirm-new-horse` 原本直接讀 DOM 當下值的邏輯不受影響，維持正常運作。同時 `js/constants.js` 的 `GAME_VERSION` 更新為 `v0.0.4`。
   驗證（v0.0.4）：`node test/smoke.js`（300輪無UI模擬，0 errors）正常跑完；`test/dom-smoke.js` 新增回歸測試：進入新馬生成畫面後於輸入框打字「測試馬」並觸發 `input` 事件，接著點選跑法（`select-style`，會呼叫 `renderApp()` 整頁重繪），確認輸入框的值仍是「測試馬」而非被重置；並在確認開始生涯後確認 `gameState.currentRun.horse.name` 正確套用為「測試馬」。另外先暫時移除 `oninput` 綁定重跑一次同一測試，確認會重現原始錯誤（輸入框被重置為「新星」），驗證測試本身真的能抓到這個問題，再還原修改。

4. **遊玩主畫面未顯示馬匹距離及跑法適性**
   紀錄版本：v0.0.3 回報（v0.0.4 沿用）→ v0.0.5 修改完成

   回報：生涯主畫面沒有顯示馬匹的距離適性與跑法適性，玩家如果忘記自己馬匹的適性，沒有地方可以重新查看。
   核對結果：`js/ui.js` 的 `renderHorsePanel()`（生涯主畫面的馬匹面板）只顯示四維屬性數值、目前主戰跑法文字與屬性上限，沒有呼叫 `renderAptitudeGrid()`／`renderStyleGrid()`。這兩個函式其實已經存在且用在新馬生成畫面（`renderNewHorseScreen()`），只是沒有一併搬到生涯主畫面，玩家進入生涯後就沒有地方能再查到當初生成時看過的距離／跑法適性等級，問題確實存在。
   修改內容（v0.0.5）：`js/ui.js` 新增 `renderStyleGridReadonly(h)`（唯讀版跑法適性格線，不帶 `data-action`，避免跟新馬生成畫面的可點選版混淆、誤讓玩家以為生涯中途能改主戰跑法）與 `renderHorseAptitudeSummary(h)`（用原生 `<details>/<summary>` 包裹距離適性格線 `renderAptitudeGrid()` 與 `renderStyleGridReadonly()`，預設收合成一行「查看距離／跑法適性」，點開才展開，避免佔用生涯主畫面版面）；`renderHorsePanel()` 在既有的主戰跑法文字下方呼叫 `renderHorseAptitudeSummary(h)`。`css/style.css` 新增 `.aptitude-summary` 相關樣式（收合列樣式、內距）。`js/constants.js` 的 `GAME_VERSION` 更新為 `v0.0.5`。
   驗證（v0.0.5）：`node test/smoke.js`（300輪無UI模擬，0 errors）與 `node test/dom-smoke.js`（jsdom全流程點擊模擬）皆正常跑完，未受影響；另外用獨立腳本開新生涯進入生涯主畫面，確認 `.aptitude-summary` 區塊確實存在、內含距離適性格線，且收合列文字為「查看距離／跑法適性」，符合預期。因為改動只新增唯讀顯示、沒有新增可點擊的 `data-action`，不影響既有的訓練/休息/出賽流程與既有測試腳本。

5. **馬匹跑法適性彼此獨立隨機生成，會出現不合理的極端組合（例如同時擅長領逃與後追）**
   紀錄版本：v0.0.4 回報 → v0.0.6 修改完成

   回報：一匹馬理論上擅長的跑法不應該同時涵蓋領逃＋後追這種對立的兩端；現實中一般只會有一種主要跑法，或是相鄰跑法的組合（領逃＋先行、先行＋居中、居中＋後追），少數馬匹會有三種相鄰跑法的組合（領逃＋先行＋居中、先行＋居中＋後追），但不應該出現跳過中間、彼此對立的組合。
   核對結果：`horse-generation.md`「跑法適性」一節原本明文規定「跑法適性維持獨立隨機生成，不與距離適性連動」，四種跑法（領逃／先行／居中／後追）各自套用同一份鐘形機率分布（S 5%／A 15%／B 35%／C 30%／D 10%／E 5%）獨立抽樣；`js/horseGen.js` 的 `generateStyleAptitude()` 也確實對 `STYLE_LIST` 四項各自呼叫 `rollStyleGrade()` 獨立抽樣，沒有任何跑法之間的關聯或互斥機制——程式碼與規格文件一致，不是撰寫錯誤。但這個獨立隨機模型確實會產生回報中提到的不合理組合：同時抽到領逃與後追都在B級以上的機率約55%×55%≈30%，都在A級以上約20%×20%=4%，並非極端罕見。這種「一匹馬同時是最前排衝刺型又是最後排後追型」在現實賽馬邏輯上互斥，會讓部分生成馬匹的跑法適性顯示不合理。與新馬戰難度問題（本文件v0.0.2問題2）性質類似：規格文件本身內部自洽，但呈現出的結果不符合直覺／現實邏輯，需要重新設計跑法適性的生成模型，而不是單純的程式錯誤修正。

   **定案方向（v0.0.6）**：比照距離適性既有的「傾向軸模型」（見 `horse-generation.md` 距離適性一節），把四種跑法放在同一條連續光譜上（領逃=1.0、先行=2.0、居中=3.0、後追=4.0），單一「主戰位置」用常態分布（平均值2.5、標準差0.7，截斷1.0-4.0，跟距離適性用同一組參數）生成，再用位置差距換算等級（沿用距離適性同一份差距/等級/加成對照表 `APTITUDE_GRADE_TABLE`）。這個方案直接讓相鄰跑法自然容易共存、對立兩端自然互斥，不需要另外設計一套新的機率表或互斥規則，也讓距離／跑法兩種適性的實作邏輯一致，比額外設計「獨立抽樣後再檢查互斥、不合就重抽」的修補式做法更乾淨。

   修改內容（v0.0.6）：
   - `js/constants.js`：`GAME_VERSION` 更新為 `v0.0.6`；`STYLE_LIST` 四個跑法各自加上 `position`（領逃1.0／先行2.0／居中3.0／後追4.0），並新增 `STYLE_ORDER` 陣列；移除原本的鐘形機率表 `STYLE_GRADE_PROB`（不再需要，等級加成改沿用 `APTITUDE_GRADE_TABLE`）。
   - `js/utils.js`：移除 `rollStyleGrade()`；`styleGradeBonus(grade)` 改為查 `APTITUDE_GRADE_TABLE`（兩份加成數字本來就完全相同，只是換一份共用的表）。
   - `js/horseGen.js`：`generateStyleAptitude(meanOverride)` 改寫成跟 `generateDistanceAptitude()` 同構的傾向軸邏輯（抽主戰位置→算每個跑法與該位置的差距→查表得等級），回傳 `{ position, grades }`；`generateHorse()` 對應新增 `aptitudes.styleMainPosition` 欄位（比照 `distanceMainPosition`），`chosenStyle` 改讀 `style.grades`。AI對手生成（`aiOpponents.js`）未改動，繼續用平均值2.5生成（規格文件沒有要求AI的跑法主戰位置跟賽事距離連動，只有距離適性才有這個規則）。
   - `horse-generation.md`：「跑法適性」一節整段改寫為「跑法傾向軸模型」，比照距離適性的光譜定位／主戰位置／差距換算等級三個小節，並附上跟距離適性對應的互斥範例。
   驗證（v0.0.6）：`node test/smoke.js`（300輪無UI模擬，0 errors）與 `node test/dom-smoke.js`（jsdom全流程點擊模擬）皆正常跑完；另外用獨立腳本產生20000匹馬統計跑法適性分布：領逃與後追（光譜兩端）同時達到A級以上的比例為0.00%、同時達到S級的比例為0.000%，確認對立兩端的高階適性已完全互斥；領逃與先行（相鄰跑法）同時達到A級以上的比例約12.66%，確認相鄰跑法仍可自然共存，符合定案方向的預期效果。
   尚未做（v0.0.6）：`state.js` 的名聲點數兌換（100點「任選一項適性等級提升一級」）目前程式碼其實從未真正讓玩家「任選」，永遠只固定微調距離主戰位置，跑法適性完全沒有被這個兌換項目觸及——這是先於本次修改就存在的落差（`horse-generation.md` 舊文字寫「跑法適性則直接提升」，但UI／`main.js`都沒有對應的選擇介面或程式分支），跟本次跑法適性生成模型的改動無關，先維持原樣，留待下一輪測試紀錄視情況另外處理。

6. **名聲點數兌換從未真的扣點，導致跨生涯累積後可能出現名聲點數過剩**
   紀錄版本：v0.0.6 回報 → v0.0.7 修改完成

   回報（v0.0.6）：名聲累積得很快，玩家遊玩多次生涯後可能會出現名聲過剩的狀況。
   核對結果（v0.0.6）：`data-structure.md` 明文規定開新馬流程是「讀取`meta.legacyFamePoints`供兌換，扣除對應點數後生成新的`currentRun`」，`game-loop.md` 也提到名聲點數的設計初衷是「避免純數值膨脹破壞挑戰性」，兩份規格文件都預期點數會隨兌換而減少。但實際核對 `js/state.js` 的 `startNewRun()`，函式只依 `fameExchangeIds` 陣列套用各檔位的效果（`stat5`／`aptitude1`／`stat3each`），從頭到尾沒有任何一行去扣減 `state.meta.legacyFamePoints`——程式碼與規格文件不符，是真的漏寫，不是設計取捨。用獨立腳本驗證：把 `legacyFamePoints` 設為150，呼叫 `startNewRun()` 並勾選全部三個檔位（門檻50+100+150，理論上需要300點、玩家點數其實不夠負擔），執行後 `legacyFamePoints` 仍是150，完全沒有扣點，確認問題存在。這代表只要玩家有兌換任何檔位，`legacyFamePoints` 實際上只會隨著每輪生涯結束（引退時 `famePointsEarned` 累加）單向往上疊加，不會因為兌換而減少，遊玩生涯數一多，累積點數自然會遠遠超過三個門檻（50/100/150）的量級，玩家會一直維持在「三項都能兌換」的狀態，跟規格文件設計的「每輪只能兌換一次、避免無限膨脹」精神不符。
   本版（v0.0.6）暫不修改：使用者已指示本次playtest回報的問題一律留到之後版本再處理，先記錄核對結果。
   修改內容（v0.0.7）：`js/state.js` 的 `startNewRun()` 在依序套用完 `stat5`／`aptitude1`／`stat3each` 三種效果後，新增依 `fameExchangeIds` 加總各檔位對應 `FAME_EXCHANGE_TIERS` 的 `threshold`（stat5=50／aptitude1=100／stat3each=150），並用 `Math.max(0, state.meta.legacyFamePoints - 加總金額)` 寫回 `state.meta.legacyFamePoints`，讓兌換後點數確實被扣除，且不會扣成負數。`js/constants.js` 的 `GAME_VERSION` 更新為 `v0.0.7`。此次修改只補上扣點本身，不處理另一個相關但獨立的既有落差——UI（`renderNewHorseScreen()` 的 `fameOptions`）判斷某檔位是否可勾選，用的是「目前尚未扣點的 `legacyFamePoints` 是否≥該檔位門檻」，玩家仍可能一次勾選多個檔位、其門檻總和超過實際持有點數（此為「非問題、純紀錄」item 1 已記錄的既有現象，非本次新增），這種情況下依本次修改會直接扣到0點而非阻擋兌換，維持目前每個檔位效果仍會照常套用；是否要進一步限制同輪只能兌換一項、或即時扣抵已勾選金額後才顯示後續檔位是否可勾，留待之後版本評估。
   驗證（v0.0.7）：`node test/smoke.js`（300輪無UI模擬，0 errors）與 `node test/dom-smoke.js`（jsdom全流程點擊模擬）皆正常跑完；另外用獨立腳本直接呼叫 `startNewRun()` 驗證四種情境：(1) 持有50點、勾選 `stat5`（門檻50）→ 執行後點數歸零；(2) 持有150點、`stat5`／`aptitude1`／`stat3each` 三檔位全勾選（門檻合計300，理論上點數不足）→ 執行後點數為0而非負數；(3) 持有80點、不勾選任何項目 → 執行後點數維持80點不變；(4) 持有200點、勾選 `stat5`＋`aptitude1`（門檻合計150）→ 執行後點數剩50點。四種情境結果皆符合預期。

7. **疲勞累積過快、休息回復不足**
   紀錄版本：v0.0.1 回報（v0.0.2、v0.0.3 沿用）→ v0.0.4 追加具體成因 → v0.0.8 修改完成

   回報：目前疲勞值累積後，靠休息只能減少一些疲勞值，且很容易在訓練後再度累積到較高值，希望調整休息時疲勞降低的幅度。
   核對結果：`js/constants.js` 中休息一次疲勞 −25，標準訓練 +18、強化訓練 +28，且疲勞沒有隨回合自然下降的機制，只能靠休息或少數正面事件降低。休息一次的效果幾乎會被緊接著一次訓練打平甚至倒賺，問題確實存在。

   回報（v0.0.4）：疲勞累積過快，加上短時間內連續參加數場賽事，導致生涯後期沒有賽事的回合幾乎都只能選擇休息，沒有餘裕再安排訓練。
   核對結果（v0.0.4）：核對 `js/state.js` 的 `doRaceAction()` 與 `js/constants.js`，比賽本身的疲勞成本＝`RACE_ACTION_BASE.fatigueDelta`(30) × 距離耐力需求係數，短距離×1.0（+30）、一哩×1.2（+36）、中距離×1.6（+48）、長距離×2.0（+60）——長距離賽事單場疲勞成本是休息回復量（−25）的兩倍以上。對照 `race-calendar.md`／`RACE_CALENDAR_FIXED`，巔峰期六場G賽事（第22、24、26、28、30、32回合）固定每兩回合一場、中間只夾一個自由回合，且刻意排了G2長距離（28回合）、G1長距離（32回合）等高疲勞成本賽事；玩家在這些空檔回合的疲勞往往已逼近或超過警示值80，光靠一次休息（−25）不足以清空，只能連續選擇休息壓低疲勞以應付下一場比賽，沒有餘裕安排訓練。這與最初回報的核心問題（休息效果被訓練/比賽打平）同源，是同一問題在巔峰期密集賽程下的具體延伸案例，確認問題存在，尚未修改。

   關聯觀察（v0.0.5）：本版新回報的「提前結束生涯比例仍高、多數為重大傷病」（見下方「待觀察」item 1 的 v0.0.5 更新）與此問題可能有間接關聯——玩家疲勞若長期偏高，代表更常處於本來就會提高整體風險感受的狀態，這點會一併留待疲勞機制調整定案後再觀察是否有改善。

   修改內容（v0.0.8）：`js/constants.js` 的 `REST_ACTION.fatigueDelta` 從 `-25` 調整為 `-35`（`training-mechanics.md` 對照表同步更新），`GAME_VERSION` 更新為 `v0.0.8`。本次只調整休息本身的疲勞回復幅度，不改動訓練/比賽的疲勞成本數字，也不新增每回合自然疲勞衰減機制——單一數值調整即可同時緩解兩個回報場景：(1) 一般情況下，休息(−35)已能完全抵銷一次強化訓練(+28)甚至標準訓練(+18)的疲勞增幅，不會再被「休息一次就被下一次訓練打平甚至倒賺」；(2) 巔峰期密集賽程下，休息回復量提高後，賽事間僅有的自由回合能讓疲勞回落得更多，減少玩家被迫連續選擇休息、完全沒有餘裕安排訓練的情況。長距離G賽（+60）單場疲勞成本仍會讓當回合疲勞衝到警示值以上乃至頂到上限，這是文件設計「巔峰期體力/疲勞管理最關鍵」的預期難度，本次修改的目標是讓疲勞在賽事之間能夠正常回落，而不是讓比賽本身完全無感。
   驗證（v0.0.8）：`node test/smoke.js`（300輪無UI模擬，0 errors）正常跑完，數值與稱號分布與修改前相近（此測試腳本為高強度隨機壓力測試，非模擬一般玩家的休息策略，主要用於確認邏輯無錯誤，不代表一般遊玩體驗）；另外用獨立腳本模擬巔峰期第21~33回合的典型賽程情境（第22/24/26/28/30/32回合依序出賽短距離/一哩/中距離/長距離/一哩/長距離，其餘回合皆選擇休息，起始疲勞基準值設為50，暫不疊加事件效果）：修改前（休息−25）疲勞軌跡為 22:55→23:30→24:66→25:41→26:89→27:64→28:100→29:75→30:100→31:75→32:100→33:75，四場G2/G1賽事中有三場（28/30/32）把疲勞直接推到上限100，且自由回合休息後只能回落到64~75，全程幾乎都在警示值80附近或以上；修改後（休息−35）疲勞軌跡為 22:45→23:10→24:46→25:11→26:59→27:24→28:84→29:49→30:85→31:50→32:100→33:65，只剩最後一場G1長距離（32回合）會頂到上限，其餘賽事後都在45~85區間，自由回合休息後能回落到10~50，多數時間落在警示值80以下，賽事之間確實有餘裕可以安排訓練，不再是「完全沒有餘裕、只能連續休息」的狀態，符合定案調整方向。
   玩家回饋（v0.0.8 playtest）：實際遊玩後回報「提高休息疲勞回復幅度有一定程度緩解疲勞累積過快問題」，與模擬結果方向一致，確認本次調整有效，暫不需要進一步加碼；若後續樣本累積後仍覺得不夠，可再評估是否加大調整幅度或另外補上每回合自然疲勞衰減機制。

8. **名聲點數兌換勾選項目合計超出目前點數時，沒有任何提示或阻擋，生涯仍會照常開始**
   紀錄版本：v0.0.8 回報 → v0.0.9 修改完成

   回報（v0.0.8）：在點選名聲點數兌換時，若選擇兌換的項目所需的名聲點數超出目前擁有的點數，生涯還是會照常進行，但理論上應該要跳出一個提示框說「兌換所需的點數超出目前擁有的點數，請重新選擇。」
   核對結果（v0.0.8）：`js/ui.js` 的 `renderNewHorseScreen()` 中，`fameOptions` 判斷每個檔位是否可勾選（`enabled`）用的是 `meta.legacyFamePoints >= t.threshold`——只拿「目前總點數」跟「該檔位單獨的門檻」比較，完全沒有把玩家已經勾選的其他檔位門檻一併加總計算，其他地方也沒有另外做加總檢查。`js/main.js` 的 `confirm-new-horse` 分支直接呼叫 `startNewRun(gameState, UI.state.previewHorse, UI.state.selectedFame)`，中間沒有任何提示視窗或阻擋邏輯；`js/state.js` 的 `startNewRun()` 也是無條件依 `fameExchangeIds` 陣列逐一套用所有已勾選檔位的效果，最後才用 `Math.max(0, legacyFamePoints - fameCost)` 把點數清到0（v0.0.7新增的扣點邏輯），但完全沒有檢查扣點後是否為負、也沒有阻擋兌換本身——扣點邏輯只負責「不讓點數變負數」，不負責「檔位門檻合計超出點數時不給兌換」。用獨立腳本驗證：把 `legacyFamePoints` 設為170，勾選 `stat5`（50）＋`aptitude1`（100）＋`stat3each`（150），門檻合計300明顯超出170，執行 `startNewRun()` 後三項效果（總點數+5隨機分配、距離適性主戰位置微調、四維各+3）皆確實完整套用，沒有拋出任何錯誤或提示，`legacyFamePoints` 直接被扣到0，生涯照常開始——問題確實存在，玩家等於能用不足額的點數換到全額效果。這個現象先前在 v0.0.3 曾以「非問題、純紀錄」item 1 的形式記錄過（當時只記錄「可以一次勾選多項」的現象，並判斷「效果加總還在可接受範圍，不算缺陷」）；現在使用者明確回報希望改為阻擋兌換並跳出提示，性質已從「純紀錄的平衡觀察」提升為「需要修正的體驗缺陷」，故另立本項記錄，原「非問題、純紀錄」item 1 予以保留並加註後續追蹤指向本項。
   本版（v0.0.8）暫不修改：使用者已指示本次playtest回報的問題一律留到之後版本再處理，先記錄核對結果。
   修改內容（v0.0.9）：問題根源在於「檢查點」放錯位置——原本只在單一檔位的勾選框上各自比對「總點數 vs 該檔位門檻」，從未在玩家真正按下「確認並開始生涯」的當下，把已勾選的所有檔位門檻加總後跟目前點數做一次總量比對。修改方式是在 `js/main.js` 的 `confirm-new-horse` 分支最前面，新增這一次總量檢查：依 `UI.state.selectedFame` 陣列查表加總各檔位的 `FAME_EXCHANGE_TIERS[].threshold`，若加總超出 `gameState.meta.legacyFamePoints`，就把 `UI.state.confirmDialog` 設為新的提示框內容（標題「點數不足」、文字「兌換所需的點數超出目前擁有的點數，請重新選擇。」）並直接 `renderApp()`、`break` 離開，不呼叫 `startNewRun()`，玩家可以回去調整勾選；點數足夠時則完全比照原本流程正常進入生涯。沿用既有的 `UI.state.confirmDialog` / `renderConfirmDialog()` 彈窗元件（原本用在強化訓練高疲勞警告），因為這個彈窗原本設計成「確定執行／取消」兩個按鈕的二選一詢問，這裡只是單純告知並要求重新選擇，不是要玩家在兩個動作間選一個，所以在 `js/ui.js` 的 `renderConfirmDialog()` 新增 `dialog.alertOnly` 旗標：為真時只渲染一個「確定」按鈕（沿用既有的 `confirm-no` 動作，點擊後單純關閉彈窗、不觸發任何 `onYes`），維持同一套彈窗樣式與互動慣例，不用另外新增一套彈窗UI。`js/constants.js` 的 `GAME_VERSION` 更新為 `v0.0.9`。
   驗證（v0.0.9）：`node test/smoke.js`（300輪無UI模擬，0 errors）正常跑完，不受影響（此測試不會觸發名聲兌換的UI流程，僅確認核心邏輯未被破壞）；`node test/dom-smoke.js`（jsdom全流程點擊模擬）正常跑完。另外新增 `test/fame-guard-smoke.js` 專門驗證本次修改：(1) 將 `legacyFamePoints` 設為170並勾選 `stat5`＋`aptitude1`＋`stat3each`（門檻合計300，超出170），點擊「確認並開始生涯」後確認彈出 `alertOnly` 提示框、文字包含「點數超出目前擁有的點數」、`gameState.currentRun` 仍為空（生涯未開始）、`legacyFamePoints` 維持170點未被扣除；(2) 點擊提示框的「確定」按鈕，確認 `confirmDialog` 正確清空；(3) 取消 `stat3each` 勾選後（門檻合計降為150，未超出170點），再次點擊「確認並開始生涯」，確認生涯正常開始（`currentRun` 建立）且點數正確扣除為20點。三種情境皆符合預期，確認阻擋邏輯只在門檻合計真的超出點數時生效，點數足夠時不影響原有兌換流程。

---

## 已確認問題，待修改

（目前無）

---

## 待觀察

1. **提前結束生涯的稀有事件觸發率／正常跑完36回合的生涯比例是否偏低**
   紀錄版本：v0.0.1 回報，v0.0.2、v0.0.3 持續觀察，v0.0.5 追加「重大傷病佔多數」的細節觀察

   回報（v0.0.1）：目前遊玩的兩個紀錄都因事件（重大傷病／天災意外／醜聞風波／伯樂相中其中之一）提前終止生涯，需要持續觀察觸發率是否過高。
   核對結果（v0.0.1）：`js/events.js` 中這組事件合計機率為 0.8%／回合，與 `events.md` 規格文件數字一致，並非計算或撰寫錯誤。已確認疲勞≥95 的強制引退判定是獨立於此的另一條路徑（且不會跳事件彈窗），所以兩次記錄應該真的是觸發到這組 0.8% 的稀有事件，不是誤判。以平均遊玩 15~20 回合估算，累積觸發機率約一到兩成，兩輪都中不算離譜但樣本數（n=2）太小，無法判斷是數值偏高還是純粹運氣不好。另外「疲勞累積過快、休息回復不足」問題如果讓玩家被迫更常做高風險行動，可能間接墊高踩到稀有事件的機會，之後可以一併觀察。
   觀察更新（v0.0.2）：目前已經有出現完整跑完正常流程（未提前結束）的生涯紀錄，但相較於提前結束的生涯，比例仍然偏低，需要繼續觀察。與 v0.0.1 是同一組現象的延伸樣本，暫不重複驗證機率算式；目前樣本數仍不足以判斷是稀有事件機率、疲勞回復機制，或兩者疊加造成的效果，先持續累積遊玩紀錄再一併判斷。
   觀察更新（v0.0.3）：持續觀察中，尚未有新的判斷依據。

   回報（v0.0.5）：提前結束生涯比例仍然較高，且多數事件是重大傷病，只有少數是其他事件。
   核對結果（v0.0.5）：核對 `js/events.js` 的 `RARE_ENDING` 陣列與 `events.md`「第一層：稀有提前結束事件」表格，四個提前結束事件在總機率0.8%／回合之下的個別佔比為：重大傷病0.3%（佔比37.5%）、天災意外0.2%（25%）、醜聞風波0.15%（18.75%）、伯樂相中0.15%（18.75%）——`resolveTurnEndEvent()` 命中這一層後，用 `pickWeightedEligible()` 依這四個 `baseProb` 做加權抽選，程式碼與規格文件數字完全一致。重大傷病本來就是這四者中機率最高的一個（是次高天災意外的1.5倍），觸發後也一律以「重大傷病」為引退原因顯示，所以「多數提前結束是重大傷病」是規格文件本身設計出來的結果，不是程式邏輯錯誤或隨機數異常；另外用 `test/smoke.js` 的高強度訓練壓力測試（300輪、大量強化訓練）觀察到的提前結束原因中，佔絕大多數的其實是疲勞≥95強制引退判定的「疲勞累積」（272/300），重大傷病等稀有事件僅佔一小部分（9/300）且四者比例大致符合上述權重，兩種路徑（疲勞強制引退 vs. 稀有事件提前結束）目前看起來獨立運作、沒有互相干擾。是否要調整「重大傷病佔比偏高」本身仍是設計取捨（例如是否要讓四種提前結束原因更平均分布），而「提前結束生涯比例整體偏高」的核心問題（本項目最初回報，n仍偏小）仍待更多實際遊玩樣本累積後再判斷，暫不修改機率數字。

   補做驗證（2026-09-16，純數據驗證，未改動程式碼，無版本更迭）：v0.0.8已針對「疲勞累積過快、休息回復不足」（見上方「已確認問題，已修改」item 7）調整休息回復幅度，用獨立腳本補做完整36回合的「合理玩家」策略模擬（不同於`test/smoke.js`的高強度隨機壓力測試），驗證這個調整對整體提前結束比例的實際影響：

   **策略A（貼近保守玩家的反應式休息）**：沒有排定賽事時，疲勞≥80才休息，其餘時間對最低項目做標準訓練（疲勞<40時30%機率改做強化訓練），有賽事必出賽。3000輪結果：休息回復−35（v0.0.8現況）與−25（v0.0.7以前）幾乎沒有差異——正常跑完36回合比例皆為4.9%（146/3000 vs 147/3000），疲勞強制引退（「疲勞累積」）都佔約84%（2526/3000 vs 2517/3000）。

   **策略B（更積極管理疲勞的玩家）**：門檻降到疲勞≥60就休息，且完全不做強化訓練，其餘條件不變。3000輪結果：休息回復−35時正常跑完比例回升到13.9%（417/3000），−25時為11.2%（337/3000），疲勞強制引退仍是最大宗（分別約70%／74%），但v0.0.8的調整這次確實看得出約2.7個百分點、相對提升近24%的正向效果。

   結論：v0.0.8的休息回復幅度調整本身方向正確、且確實對「願意提早休息的玩家」有實質幫助（策略B），但對「疲勞逼近警示值80才反應」的玩家（策略A）幾乎無感——因為多場賽事（尤其巔峰期G賽、以及新星期/成長期的新馬戰、未勝利賽、公開賽等固定賽程）本身疲勞成本就很高，若等到警示值才休息，賽事當天疲勞經常已被推到臨界值95以上，觸發20%／回合的強制引退判定，光靠單次休息回復幅度調整難以完全補償。即使是採取policy B的較積極策略，提前結束比例仍高達86%左右，遠高於「大多數生涯應該能正常跑完」的直覺預期，顯示疲勞機制在整個36回合尺度下（而不只是先前只驗證過的巔峰期21-33回合片段）仍然偏嚴苛，可能需要更根本的調整（例如額外的每回合自然疲勞衰減、或降低部分賽事的疲勞成本），而不只是持續微調休息回復數字。本次只記錄驗證數據與策略敏感度分析，不預設修改方向，留待之後版本評估；本項（提前結束生涯比例）維持「待觀察」狀態，不觸發版本更迭。

2. **新馬戰改自選距離（v0.0.3）後，「選自己最擅長的距離」策略讓玩家整體贏面明顯超過公平基準，且各品質等級差距被拉近**
   紀錄版本：2026-09-16 補做機率驗證時發現

   觀察：詳見上方「已確認問題，已修改」item 2 的「補做驗證（2026-09-16）」段落。用獨立腳本模擬「自選最擅長距離」打法，24000輪結果顯示新馬戰第一名機率：不良32.6%／普通39.7%／優良46.5%／稀有52.6%，整體加權平均37.8%，全面超過6匹馬公平隨機的1/6（≈16.7%）基準，其中不良馬漲幅最大（原本5.5%→ 32.6%）。成因是玩家可以精準選到跟自己適性主戰位置最近的距離分類（差距必定≤0.5，幾乎穩拿A級以上加成），但AI對手的主戰位置是以該距離分類座標為平均值重新隨機生成，貼合程度是隨機的，不像玩家一樣「保證貼近」，形成玩家在自選主場上的結構性優勢。這跟v0.0.2最初核對出的落差方向相反（原本是AI天生比玩家貼合賽事距離，現在變成玩家天生比AI貼合自己選的距離）。
   目前判斷：這是否算「新的難度問題」見仁見智——一方面數字上確實明顯偏離公平基準，尤其不良馬也能拿到32.6%第一名機率，可能讓新馬戰對熟悉機制的玩家來說偏簡單；另一方面這也可以視為「玩家做出正確策略選擇應得的合理回報」，畢竟游戲鼓勵玩家先看適性格線再決定怎麼玩。是否需要調整（例如AI主戰位置改用更集中的分布、或拉近AI跟該距離的貼合度）留待之後版本視實際玩家回報再決定，本次只記錄驗證數據與成因分析，不預設修改方向。

---

## 非問題、純紀錄

1. **名聲點數兌換目前可一次勾選多項，未來項目變多時要留意平衡**
   紀錄版本：v0.0.3 回報

   回報：目前名聲點數可以一次勾選多個兌換項目；若之後繼續新增兌換項目，同一輪生涯疊加兌換太多項可能會影響平衡。
   核對結果：`js/main.js` 的 `toggle-fame` 分支與 `js/ui.js` 的 fameOptions 渲染，目前確實沒有任何互斥或數量限制邏輯，`startNewRun()` 也是依序套用 `fameExchangeIds` 陣列裡的每一項效果，技術上玩家現在就能一次全選三項。目前3個選項效果各自獨立、累加起來還在可接受範圍，不算原型階段的缺陷，先記錄下來，之後擴充兌換項目時要一併評估是否需要限制（例如每輪限兌換1項、拉高點數門檻）。
   後續追蹤（v0.0.8）：使用者針對「勾選檔位合計超出目前點數」的情況明確回報需要跳出提示阻擋，已另立「已確認問題，待修改」item 1 記錄與處理，本項維持原記錄，僅供背景參考。
   後續追蹤（v0.0.9）：上述阻擋邏輯已於 v0.0.9 修改完成，詳見「已確認問題，已修改」item 8。本項（可一次勾選多項本身）維持原記錄，僅供背景參考，未來項目變多時仍可視情況評估是否需要進一步限制。

2. **閘位系統構想**：比賽名次目前跟賽道順位高度連動，缺少懸念
   紀錄版本：v0.0.3 回報

   回報：目前比賽過程中，每匹馬在賽道上的相對順位跟牠的最終名次高度連動，玩家容易提前看出比賽結果。構想是之後加入「閘位系統」：比賽開始前隨機抽籤決定各馬出發閘位，閘位可能對不同跑法產生優劣（例如內側閘位有利領逃、外側閘位有利後追），增加比賽隨機性與策略深度。
   核對結果：純屬新功能構想，不是目前原型的缺陷，`race-simulation.md` 目前也沒有規劃閘位機制，已同步記錄到 `待辦系統.md`，之後排優先順序時可以參考。
