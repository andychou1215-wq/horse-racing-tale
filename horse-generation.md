# 馬匹初始屬性生成規則

## 核心屬性生成（速度/耐力/爆發力/幸運-穩定性）

採「總點數+隨機分配」模式，讓馬匹有品質差異也有特化差異。

### 第一步：品質等級決定總點數區間

| 品質等級 | 總點數區間 | 出現機率 |
|---|---|---|
| 不良 | 100-115 | 50% |
| 普通 | 116-130 | 30% |
| 優良 | 131-145 | 15% |
| 稀有 | 146-160 | 5% |

在對應區間內均勻抽取實際總點數。

### 第二步：分配到四項屬性
每項先給地板值15（避免0值畸形馬），剩餘點數（總點數 - 4×15）用隨機權重比例分配：生成4個隨機數正規化為權重比例，依比例分配剩餘點數，四捨五入誤差歸給幸運/穩定性。

同一品質等級的馬也可能出現「單項特化」或「四維均衡」的不同個體，增加馬匹辨識度。

## 距離適性（距離傾向軸模型）

短距離與長距離在生理特化上是相反方向（衝刺型 vs 耐力型），不應該獨立隨機生成、彼此無關。改用「距離傾向軸」模型，讓距離適性之間自然互斥。

### 光譜定位
把四個距離分類放在一條連續光譜上：短距離=1.0、一哩=2.0、中距離=3.0、長距離=4.0。

### 生成主戰位置
每匹馬生成時抽一個「主戰位置」（該馬最擅長的距離點，可落在分類之間的小數位置），採常態分布：平均值2.5、標準差0.7，並截斷在1.0-4.0之間。讓「一哩~中距離全能馬」最常見，純短距離衝刺型或純長距離耐力型都是相對少數，符合現實賽馬的分布狀況。

### 依主戰位置換算各分類適性等級
計算每個距離分類位置與主戰位置的差距(diff = |分類位置 - 主戰位置|)，差距越大等級越低：

| 差距(diff) | 等級 | 比賽數值加成 |
|---|---|---|
| ≤ 0.3 | S | +10% |
| ≤ 0.8 | A | +5% |
| ≤ 1.3 | B | 0%（基準） |
| ≤ 1.8 | C | -5% |
| ≤ 2.3 | D | -10% |
| > 2.3 | E | -15% |

範例：主戰位置=1.0（純短距離特化馬）→ 短距離diff=0(S)、一哩diff=1(B)、中距離diff=2(D)、長距離diff=3(E)，短距離和長距離的高階適性自然互斥。主戰位置=2.5（全能型）→ 一哩/中距離都能拿到不錯等級，短距離和長距離則相對平庸(B~C)，呈現「樣樣通樣樣鬆」的全能馬特色。

加成套用在 race-simulation.md 的「距離耐力需求係數」計算上（例如長距離適性S可抵銷部分長距離耐力懲罰）。

## 跑法適性（跑法傾向軸模型，v0.0.6起）

跑法（領逃/先行/居中/後追）原本採四項各自獨立隨機生成，但實測發現會出現不合理的極端組合（例如同一匹馬同時是最前排衝刺型的領逃高手、又是最後排的後追高手）——領逃與後追在跑法策略上是相反方向的兩端，不該彼此獨立、毫無關聯。因此比照距離適性，改採「跑法傾向軸」模型，讓相鄰跑法自然容易共存、對立兩端自然互斥。

### 光譜定位
把四種跑法放在一條連續光譜上：領逃=1.0、先行=2.0、居中=3.0、後追=4.0。

### 生成主戰位置
每匹馬生成時抽一個「主戰位置」（該馬最擅長的跑法光譜點，可落在分類之間的小數位置），採常態分布：平均值2.5、標準差0.7，並截斷在1.0-4.0之間，與距離適性用同一組參數，理由相同：讓「先行~居中」全能型最常見，純領逃或純後追型都是相對少數。

### 依主戰位置換算各跑法適性等級
計算每個跑法位置與主戰位置的差距(diff = |跑法位置 - 主戰位置|)，套用跟距離適性完全相同的差距/等級/加成對照表：

| 差距(diff) | 等級 | 比賽數值加成 |
|---|---|---|
| ≤ 0.3 | S | +10% |
| ≤ 0.8 | A | +5% |
| ≤ 1.3 | B | 0%（基準） |
| ≤ 1.8 | C | -5% |
| ≤ 2.3 | D | -10% |
| > 2.3 | E | -15% |

範例：主戰位置=1.0（純領逃特化馬）→ 領逃diff=0(S)、先行diff=1(B)、居中diff=2(D)、後追diff=3(E)，領逃和後追的高階適性自然互斥，不會再同時出現兩端都是A/S的怪異組合。主戰位置=2.5（全能型）→ 先行/居中都能拿到不錯等級，領逃和後追則相對平庸(B~C)。

加成套用在對應跑法的階段係數上（例如領逃適性A會加成領逃跑法在起跑階段的係數）。

距離適性與跑法適性合起來讓玩家在選擇跑法/報名賽事時有「揚長避短」的策略空間，不只是看四維屬性高低。

## 跨輪迴的名聲點數兌換

呼應 game-loop.md 的輪迴銜接機制。引退時獲得的名聲點數，可在下一輪開新馬時兌換初始加成，每輪只能兌換一次（避免無限膨脹）：

| 兌換門檻 | 效果 |
|---|---|
| 50點 | 總屬性點數額外+5（套用同樣的隨機分配邏輯） |
| 100點 | 任選一項適性等級提升一級（距離適性提升會微調主戰位置往該分類靠攏，而非直接改單一等級，以維持光譜互斥邏輯；跑法適性則直接提升） |
| 150點 | 四項核心屬性各直接+3（不重新分配，直接加總） |

三個檔位彼此獨立，名聲點數足夠時理論上可三者兼得，但每輪加成幅度刻意壓在小範圍內，且最終數值仍受下方全域上限規則約束。

## 屬性數值上限規則

- **永久屬性上限：160**。每項核心屬性的永久數值（初始生成 + 訓練成長 + 永久性事件加成如「覺醒時刻」+ 名聲點數兌換）最高不可超過160，超過部分無效。
- **暫時屬性上限：170**。疊加當下暫時加成（比賽當回合buff，如粉絲應援、天時地利、完美體態等）後的瞬時數值最高不可超過170，即暫時加成空間最多為永久上限之上再+10。

此上限規則與 training-mechanics.md 的屬性成長公式共用同一組數字，確保兩份文件數值一致。
