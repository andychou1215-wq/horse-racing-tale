"""對照表 — 各項倍率/等級換算表。

數值來源：docs/ 內已定案的規則；部分倍率(標記「假設值」)在 docs 中只有
文字描述（如「明顯加成/小幅加成/標準/小幅懲罰/明顯懲罰」），沿用
`數值平衡試算.xlsx` 對照表分頁選定的具體測試數字，正式平衡數值仍待
後續試玩調整（見 GDD 第12節「開放的設計取捨」）。
"""
from __future__ import annotations

Grade = str  # "S" | "A" | "B" | "C" | "D"
Pace = str  # "逃" | "先" | "差" | "追"

# 狀態(賽前分級) -> 比賽公式.md 狀態倍率
STATUS_MULTIPLIER: dict[Grade, float] = {
    "S": 1.2,
    "A": 1.1,
    "B": 1.0,
    "C": 0.9,
    "D": 0.8,
}

# 距離/場地適性等級 -> 倍率（馬匹系統.md 只給文字描述，此處為試算表假設值）
AFFINITY_MULTIPLIER: dict[Grade, float] = {
    "S": 1.15,
    "A": 1.05,
    "B": 1.0,
    "C": 0.95,
    "D": 0.85,
}

# 跑法適配度（比賽公式.md 外部乘數層，六階段皆套用，彎道固定1.00）
PACE_STAGE_MULTIPLIER: dict[Pace, dict[str, float]] = {
    "逃": {"起跑": 1.10, "前段": 1.15, "中段": 1.00, "後段": 0.90, "彎道": 1.00, "衝刺": 0.85},
    "先": {"起跑": 0.90, "前段": 1.05, "中段": 1.10, "後段": 1.00, "彎道": 1.00, "衝刺": 0.90},
    "差": {"起跑": 0.90, "前段": 1.00, "中段": 1.10, "後段": 1.05, "彎道": 1.00, "衝刺": 0.90},
    "追": {"起跑": 0.85, "前段": 0.90, "中段": 1.00, "後段": 1.10, "彎道": 1.00, "衝刺": 1.15},
}

# 跑法修正（體力系統.md「跑法修正」，只涵蓋有消耗計算的五個階段）
PACE_STAMINA_MULTIPLIER: dict[Pace, dict[str, float]] = {
    "逃": {"起跑": 1.15, "前段": 1.20, "中段": 1.00, "後段": 0.90, "衝刺": 0.85},
    "先": {"起跑": 1.00, "前段": 1.05, "中段": 1.05, "後段": 1.00, "衝刺": 0.95},
    "差": {"起跑": 0.95, "前段": 0.95, "中段": 1.00, "後段": 1.05, "衝刺": 1.05},
    "追": {"起跑": 0.90, "前段": 0.90, "中段": 0.95, "後段": 1.05, "衝刺": 1.15},
}

# 基準體力曲線（體力系統.md），與相鄰階段差值（基礎消耗）
BASE_REMAINING_STAMINA: dict[str, float] = {
    "起跑": 100,
    "前段": 88,
    "中段": 64,
    "後段": 37,
    "衝刺": 12,
}

BASE_STAMINA_CONSUMPTION: dict[str, float] = {
    "前段": 12,
    "中段": 24,
    "後段": 27,
    "衝刺": 25,
}

# 比賽六階段的固定順序
RACE_STAGES: tuple[str, ...] = ("起跑", "前段", "中段", "後段", "彎道", "衝刺")

# 年齡成長階段 -> 成長倍率（馬匹成長.md）
AGE_STAGE_MULTIPLIER: dict[str, float] = {
    "上升期": 1.15,
    "巔峰期": 1.00,
    "停滯期": 0.6,
    "衰退期": 0.3,
}
